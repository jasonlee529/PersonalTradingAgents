## 1. 后端数据与 API

- [x] 1.1 在 `src/api/models.py` 增加涨停股列表响应模型，包含股票代码、名称、市场、交易日、价格、涨跌幅、成交量/成交额以及可选封板相关字段
- [x] 1.2 在行情数据源层封装获取指定交易日涨停池数据的方法，并统一输出字段命名
- [x] 1.3 在 `DataCollector` 增加 `get_limit_up_stocks(trade_date, market)` 方法，复用现有缓存并处理数据源失败
- [x] 1.4 实现 A 股主板过滤规则，保留沪市主板和深市主板，排除创业板、科创板、北交所等非主板代码
- [x] 1.5 在 `src/api/routers/stocks.py` 增加涨停股列表端点，支持 `trade_date`、`market`、`q`、`limit`、`offset` 查询参数
- [x] 1.6 为后端涨停股 API 添加单元测试，覆盖主板过滤、关键字筛选、空结果和数据源异常场景

## 2. 前端 API 与页面

- [x] 2.1 在 `web/src/api/client.ts` 为 `stockApi` 增加涨停股列表查询方法和对应 TypeScript 数据类型
- [x] 2.2 新增每日主板涨停股页面，包含交易日选择、市场范围选择、关键字搜索、刷新按钮和列表表格
- [x] 2.3 在列表中展示核心行情字段，并对缺失的可选字段显示占位值
- [x] 2.4 实现加载状态、错误提示、空结果状态和重新加载交互
- [x] 2.5 为列表中的股票代码/名称添加跳转到 `/stock?symbol=<symbol>` 的链接

## 3. 行情详情联动与导航

- [x] 3.1 更新 `StockDetailPage`，支持从 URL query 读取 `symbol` 并加载对应股票详情，即使该股票不在持仓中
- [x] 3.2 保持无 URL symbol 时原有持仓默认选择逻辑不变
- [x] 3.3 在 `web/src/App.tsx` 注册新涨停股页面路由
- [x] 3.4 在 `web/src/components/Layout.tsx` 增加“涨停池”导航入口，并确保当前路由高亮正确

## 4. 验证

- [x] 4.1 运行后端相关测试，确认涨停股 API、主板过滤和详情接口未回归
- [x] 4.2 运行前端类型检查/构建，确认新页面和 API 类型通过
- [x] 4.3 手动验证选择交易日后能看到主板涨停股列表，并且点击任一股票可进入对应行情详情
